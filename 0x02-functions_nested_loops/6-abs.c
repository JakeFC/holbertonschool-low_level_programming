#include "holberton.h"
#include <stdlib.h>

/**
 * _abs - prints the absolute value of an int
 * Return: the value of int
 * c is input
 * a is abs of c
 */
int _abs(int c)
{
	int a = abs(c);

	return (a);
}
